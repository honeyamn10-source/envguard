# envguard

Audit your `.env` files and scan your codebase for leaked secrets.

This fixture repository is used by the automated test-suite. It contains a
deliberately planted fake secret so the tests can assert exact findings.