name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Python ${{ matrix.python }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Install
        run: python -m pip install --upgrade pip pytest && python -m pip install -e .

      - name: Run tests
        run: python -m pytest tests -q

  cli-smoke:
    name: CLI smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: python -m pip install --upgrade pip pytest && python -m pip install -e .

      - name: Version check
        run: envguard --version

      - name: Scan a clean fixture and validate the installed CLI
        run: |
          mkdir -p /tmp/envguard-clean
          printf 'print("hello")\n' > /tmp/envguard-clean/app.py
          envguard scan /tmp/envguard-clean --severity critical
          envguard scan /tmp/envguard-clean --format json